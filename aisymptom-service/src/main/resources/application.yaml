server:
  port: 8089
spring:
  application:
    name: aisymptom-service

internal:
  api:
    key: your-secret-internal-key

gemini:
  api:
   key: AIzaSyAzvZATEFFFQHwFtu9IGYoieRUFJgkCTjE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8098/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}